packages:
  - "packages/*"
  - "apps/*"
allowBuilds:
  # Byreal CLI's optional native deps — pure-JS fallback is fine, don't build.
  bigint-buffer: false
  bufferutil: false
  utf-8-validate: false
  esbuild: true
  sharp: true
  keccak: false
